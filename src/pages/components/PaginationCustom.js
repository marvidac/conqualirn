import React, { Component, useState } from 'react';
import { Container, Content, Input, Item, Label } from "native-base";

export default class PaginationCustom extends Component {
    [bosta, setBosta]  = useState([]);
    
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    componentDidMount() {

    }

    render() {

        return (
            <Item floatingLabel style={{ paddingTop: 10}}>
                
            </Item>
        );
    }
}